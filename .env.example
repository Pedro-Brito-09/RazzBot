# Copy to /etc/razzbot.env on the server (chmod 600, owned by root)
TOKEN=your_discord_bot_token_here
API_KEY=your_roblox_open_cloud_api_key_here
# Optional. Set to your server's ID so slash commands register instantly.
# Leave unset to register globally (takes up to an hour to appear).
GUILD_ID=
