# 2.6 is the floor for Components V2 (LayoutView / Container / Section).
discord.py>=2.6
# Pinned deliberately. aiohttp 3.14.3 hangs forever waiting on response
# headers from apis.roblox.com (verified: 180s timeout), while 3.10.11
# returns the same request in 0.3s from the same host. Do not relax this
# to a range without re-testing against the Open Cloud datastore API.
aiohttp==3.10.11
zstandard>=0.22
